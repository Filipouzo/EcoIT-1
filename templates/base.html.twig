<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}EcoIT -  La plateforme de formation leader de l'éco-conception{% endblock %}</title>
    <link href="https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Poppins&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">   
    <link rel="stylesheet" href="{{ asset('assets/css/ecoit.css') }}">   
</head>
<body>
    <header>
        <div class="logo">
            <a href="{{ path('app_home') }}">EcoIT</a>
        </div>
        <nav class="navbar">
            <div class="navbar-menu-block">
                <span class="navbar-menu-item">
                    <a href="{{ path('app_home') }}">Accueil</a>
                </span>
            </div>
            <div class="navbar-menu-block">
                <span class="navbar-menu-item">
                    <a href="#">Formations</a>
                </span>
            </div>
            {% if app.user %}
                {% if app.user.roles is same as (['ROLE_LEARN']) %}
                <div class="navbar-menu-block">
                    <span class="navbar-menu-item">
                        <a href="{{ path('app_account') }}">Mon compte</a>
                    </span>
                </div>
                <div class="navbar-menu-block">
                    <span class="navbar-menu-item">
                        <a href="{{ path('app_logout') }}">Deconnexion</a>
                    </span>
                </div>
                {% else %}
                <div class="navbar-menu-block">
                    <span class="navbar-menu-item">
                        <a href="{{ path('admin') }}">Mon espace</a>
                    </span>
                </div>
                <div class="navbar-menu-block">
                    <span class="navbar-menu-item">
                        <a href="{{ path('app_logout') }}">Deconnexion</a>
                    </span>
                </div>
                {% endif %}
            {% else %}            
            <div class="navbar-menu-block">
                <span class="navbar-menu-item">
                    <a href="{{ path('app_login')}}">Connexion</a>
                </span>
            </div>
            <div class="navbar-menu-block">
                <span class="navbar-menu-item">
                    <a href="{{ path('app_register') }}">Devenir apprenant</a>
                </span>
            </div>
            <div class="navbar-menu-block">
                <span class="navbar-menu-item">
                    <a href="{{ path('app_instructor') }}">Devenir instructeur</a>
                </span>
            </div>
            {% endif %}
        </nav>
    </header>
    <main>
    {% block content %}
    {% endblock %}
    </main>
    <footer>
        <p>Mon footer</p>
    </footer>
</body>
</html>
